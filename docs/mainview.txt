<Window x:Class="RhonAyro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RhonAyro"
        xmlns:vm="clr-namespace:RhonAyro"
        Title="RhonAyro" Width="1200" Height="800">

    <Window.DataContext>
        <!-- Set in code-behind; left here empty to avoid design-time DI issues -->
    </Window.DataContext>

    <Window.Resources>
        <!-- Header: show title + close button -->
        <DataTemplate DataType="{x:Type local:TabItemHost}" x:Key="TabHeaderTemplate">
            <DockPanel LastChildFill="False" Margin="4,0">
                <TextBlock Text="{Binding Header}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <Button Content="×"
                        Width="18" Height="18" Padding="0"
                        Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                        CommandParameter="{Binding}"
                        ToolTip="Close"/>
            </DockPanel>
        </DataTemplate>

        <!-- Content: display the VM, which the next DataTemplates map to views -->
        <DataTemplate DataType="{x:Type local:TabItemHost}" x:Key="TabContentTemplate">
            <ContentControl Content="{Binding ViewModel}"/>
        </DataTemplate>

        <!-- VM→View mappings -->
        <DataTemplate DataType="{x:Type vm:PreparationViewModel}">
            <local:PreparationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StartListViewModel}">
            <local:StartListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CompetitionViewModel}">
            <local:CompetitionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScoreboardViewModel}">
            <local:ScoreboardView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <TabControl ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}"
                    ItemTemplate="{StaticResource TabHeaderTemplate}"
                    ContentTemplate="{StaticResource TabContentTemplate}"/>
    </Grid>
</Window>
